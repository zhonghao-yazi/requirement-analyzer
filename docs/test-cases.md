# 需求分析测试用例生成平台 — 测试用例文档

> 版本：v1.0 | 日期：2026-08-03 | 编写人：测试开发工程师
>
> 覆盖范围：页面加载、文件上传、分析流程、需求摘要、流程图、测试用例表格、分类筛选、
> Excel 下载、复制表格、错误处理、边界条件、状态管理、后端 API、安全性、兼容性

---

## 用例统计总览

| 模块 | 用例数 | 核心流程 | 边界值 | 安全性 | 稳定性 |
|------|--------|---------|--------|--------|--------|
| 1. 页面加载与初始渲染 | 8 | 5 | 3 | 0 | 0 |
| 2. 文件上传（点击选择） | 9 | 4 | 5 | 0 | 0 |
| 3. 文件上传（拖拽） | 7 | 3 | 4 | 0 | 0 |
| 4. 文件格式校验 | 10 | 3 | 6 | 1 | 0 |
| 5. 分析流程 | 8 | 5 | 2 | 0 | 1 |
| 6. 需求摘要展示 | 6 | 3 | 3 | 0 | 0 |
| 7. 核心流程图 | 9 | 4 | 3 | 0 | 2 |
| 8. 测试用例表格 | 12 | 5 | 5 | 0 | 2 |
| 9. 分类筛选 | 10 | 4 | 5 | 0 | 1 |
| 10. Excel 下载 | 8 | 3 | 3 | 1 | 1 |
| 11. 复制表格 | 6 | 3 | 2 | 0 | 1 |
| 12. 错误处理 | 9 | 3 | 4 | 2 | 0 |
| 13. 状态管理与交互 | 8 | 4 | 3 | 0 | 1 |
| 14. 后端 API | 10 | 4 | 3 | 2 | 1 |
| 15. 兼容性与性能 | 7 | 2 | 2 | 0 | 3 |
| **合计** | **127** | **60** | **51** | **6** | **14** |

---

## 1. 页面加载与初始渲染（8 条）

### 1.1 页面标题正确显示

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证浏览器标签页标题包含"需求分析" |
| **执行步骤** | 1. 打开浏览器，访问 `http://localhost:5173` 2. 查看浏览器标签页标题 |
| **预期结果** | 标签页标题包含"需求分析"字样 |

### 1.2 Header 品牌区域完整渲染

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证页面顶部 Header 区域显示品牌名称和副标题 |
| **执行步骤** | 1. 访问首页 2. 检查页面顶部 Header 区域 |
| **预期结果** | 显示标题"需求分析 · 测试用例生成平台"；显示副标题"上传需求文件，智能分析，一键生成完整测试用例" |

### 1.3 上传区域默认显示上传引导文案

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证未上传文件时，上传区域显示拖拽引导文案 |
| **执行步骤** | 1. 访问首页 2. 查看文件上传区域 |
| **预期结果** | 显示"拖拽文件到此处，或"文案；显示"点击选择"链接文字；显示支持的格式列表（PNG/JPG/GIF/DOCX/PDF/MD/TXT/XMind） |

### 1.4 格式标签全部展示

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证上传区域下方展示全部 6 种格式标签 |
| **执行步骤** | 1. 访问首页 2. 查看上传区域下方的格式标签区域 |
| **预期结果** | 依次显示 6 个格式标签：图片（蓝色）、文档（绿色）、PDF（红色）、Markdown（紫色）、文本（橙色）、XMind（粉色）；每个标签颜色与设计规范一致 |

### 1.5 初始空状态提示

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证未上传文件时，页面显示空状态引导文案 |
| **执行步骤** | 1. 访问首页 2. 查看上传区域下方 |
| **预期结果** | 显示"上传需求文件，AI 将自动分析并生成测试用例"空状态提示文案 |

### 1.6 初始状态不显示结果区域

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证未上传文件时，结果区域（需求摘要/流程图/测试用例表格/下载栏）全部隐藏 |
| **执行步骤** | 1. 访问首页 2. 检查页面中是否存在 h2 标题元素 3. 检查是否存在表格、流程图、下载按钮 |
| **预期结果** | 页面中不存在任何 h2 标题；不存在表格元素；不存在下载按钮；不存在复制按钮 |

### 1.7 页面加载无控制台错误

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证页面首次加载时浏览器控制台无 JavaScript 异常 |
| **执行步骤** | 1. 打开浏览器开发者工具（F12）→ Console 面板 2. 访问首页 3. 等待页面完全加载 4. 检查控制台输出 |
| **预期结果** | 控制台无红色的 JavaScript 错误（error 级别日志） |

### 1.8 全局 CSS 主题变量生效

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证页面加载了淡紫色主题的 CSS 变量 |
| **执行步骤** | 1. 访问首页 2. 打开开发者工具 → Elements 面板 3. 检查 `:root` 或 `body` 的计算样式（Computed） |
| **预期结果** | `--color-primary` 值为 `#7e57c2`；`--color-bg` 值为 `#faf8fc`；`--color-text` 值为 `#2d2d2d` |

---

## 2. 文件上传 — 点击选择（9 条）

### 2.1 点击上传区域弹出文件选择框

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证点击上传区域可触发系统文件选择对话框 |
| **执行步骤** | 1. 访问首页 2. 点击上传区域（"拖拽文件到此处"文字所在区域） |
| **预期结果** | 系统弹出文件选择对话框；对话框的文件类型筛选器包含支持的格式 |

### 2.2 上传 .txt 文件成功显示文件名

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证选择 .txt 文件后，上传区域显示文件名 |
| **执行步骤** | 1. 访问首页 2. 通过点击上传区域选择 `tests/test-data/test_sample.txt` 文件 |
| **预期结果** | 上传区域变为文件信息展示状态；显示文件名 `test_sample.txt`；显示文件大小 |

### 2.3 上传 .md 文件成功显示文件名

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证选择 .md 文件后，上传区域正确展示 |
| **执行步骤** | 1. 访问首页 2. 通过点击上传区域选择一个 .md 文件 |
| **预期结果** | 上传区域显示 .md 文件名；显示文件大小 |

### 2.4 上传 .docx 文件成功显示文件名

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证选择 .docx 文件后，上传区域正确展示 |
| **执行步骤** | 1. 访问首页 2. 通过点击上传区域选择一个 .docx 文件 |
| **预期结果** | 上传区域显示 .docx 文件名；显示文件大小 |

### 2.5 上传 .pdf 文件成功显示文件名

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证选择 .pdf 文件后，上传区域正确展示 |
| **执行步骤** | 1. 访问首页 2. 通过点击上传区域选择一个 .pdf 文件 |
| **预期结果** | 上传区域显示 .pdf 文件名；显示文件大小 |

### 2.6 上传 .xmind 文件成功显示文件名

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证选择 .xmind 文件后，上传区域正确展示 |
| **执行步骤** | 1. 访问首页 2. 通过点击上传区域选择一个 .xmind 文件 |
| **预期结果** | 上传区域显示 .xmind 文件名；显示文件大小 |

### 2.7 上传图片文件成功显示文件名

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证选择图片文件（PNG/JPG/GIF）后，上传区域正确展示 |
| **执行步骤** | 1. 访问首页 2. 通过点击上传区域选择一个 .png 文件 |
| **预期结果** | 上传区域显示图片文件名；显示文件大小 |

### 2.8 文件大小格式化显示正确

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证不同大小的文件以合适的单位显示（B / KB / MB） |
| **执行步骤** | 1. 上传一个小于 1024 字节的文件 2. 查看文件大小显示 3. 清除后上传一个 10KB 左右的文件 4. 查看文件大小显示 5. 清除后上传一个 2MB 左右的文件 6. 查看文件大小显示 |
| **预期结果** | 小文件显示单位为 "B"（如 "856 B"）；中等文件显示单位为 "KB"（如 "10.5 KB"）；大文件显示单位为 "MB"（如 "2.3 MB"）；小数保留 1 位 |

### 2.9 重复选择同一文件可再次触发

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证清除后可以再次选择同一个文件而不被浏览器缓存阻止 |
| **执行步骤** | 1. 上传文件 A 2. 点击清除按钮移除 3. 再次上传同一个文件 A |
| **预期结果** | 第二次上传同样成功显示文件名；`input[type="file"]` 的 value 被正确重置 |

---

## 3. 文件上传 — 拖拽（7 条）

### 3.1 拖拽文件到上传区成功识别

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证从操作系统拖拽文件到上传区域后，文件被正确识别 |
| **执行步骤** | 1. 访问首页 2. 从文件管理器拖拽一个 .txt 文件到上传区域 3. 松开鼠标完成拖放 |
| **预期结果** | 上传区域显示拖入的文件名；文件状态正确切换 |

### 3.2 拖拽悬停时出现高亮效果

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证文件拖入上传区域上方（未释放）时，出现视觉高亮反馈 |
| **执行步骤** | 1. 访问首页 2. 从文件管理器拖拽一个文件进入上传区域（不释放鼠标） 3. 观察上传区域的边框/背景变化 |
| **预期结果** | 上传区域边框变为高亮色（淡紫色 `#7e57c2`）；背景色变浅；或出现其他明显的视觉反馈效果 |

### 3.3 拖拽离开时高亮效果消失

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证文件从上传区域拖出后，高亮效果恢复正常 |
| **执行步骤** | 1. 访问首页 2. 拖拽文件进入上传区域（出现高亮） 3. 将文件拖出上传区域 4. 观察上传区域样式 |
| **预期结果** | 上传区域恢复到默认样式；高亮效果消失 |

### 3.4 拖拽多个文件仅取第一个

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证同时拖拽多个文件时，仅处理第一个文件 |
| **执行步骤** | 1. 访问首页 2. 同时选中 3 个文件拖入上传区域 |
| **预期结果** | 仅显示第一个文件的文件名；不显示其他文件信息 |

### 3.5 分析中拖拽文件不触发上传

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证在 AI 分析进行中时，拖拽新文件不会被处理 |
| **执行步骤** | 1. 上传一个文件触发分析（进入 loading 状态） 2. 在 loading 期间拖拽另一个文件到上传区域 |
| **预期结果** | 拖拽操作被忽略；不会替换当前正在分析的文件；不会中断分析流程 |

### 3.6 分析中拖拽不出现高亮

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证分析进行中拖入文件时不显示高亮效果 |
| **执行步骤** | 1. 上传文件触发分析 2. 在 loading 显示期间，拖拽文件进入上传区域 |
| **预期结果** | 上传区域不显示高亮（dragging 类不生效） |

### 3.7 分析中点击上传区域无效

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证分析进行中点击上传区域不会弹出文件选择框 |
| **执行步骤** | 1. 上传文件触发分析 2. 在 loading 期间点击上传区域 |
| **预期结果** | 文件选择对话框不弹出 |

---

## 4. 文件格式校验（10 条）

### 4.1 上传不支持的格式显示错误提示

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证上传不支持格式的文件时，显示明确的错误提示 |
| **执行步骤** | 1. 访问首页 2. 上传一个 .exe 文件 |
| **预期结果** | 上传区域下方显示错误提示：`不支持的文件格式 ".exe"` |

### 4.2 上传不支持格式时不触发分析流程

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证上传不支持格式时，不触发 loading 和分析流程 |
| **执行步骤** | 1. 访问首页 2. 上传一个 .rar 文件 3. 检查页面状态 |
| **预期结果** | 不出现"正在解析文件，AI 分析需求内容..."加载文案；不出现 loading spinner；结果区域保持隐藏 |

### 4.3 格式错误后可重新上传有效文件

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证格式校验失败后，重新上传正确格式文件可以成功 |
| **执行步骤** | 1. 先上传 .exe 文件（触发格式错误） 2. 错误提示出现后，上传 .txt 文件 |
| **预期结果** | 之前的格式错误提示消失；新上传的有效文件正常显示；自动触发分析流程 |

### 4.4 无扩展名文件处理

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证没有扩展名的文件上传时的处理 |
| **执行步骤** | 1. 访问首页 2. 上传一个没有扩展名的文件（如 "README"） |
| **预期结果** | 若非图片 MIME 类型，显示"不支持的文件格式"错误；不会触发分析 |

### 4.5 未知扩展名但为图片 MIME 类型的处理

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证扩展名不在支持列表但 MIME 为 image/* 的文件可被接受 |
| **执行步骤** | 1. 访问首页 2. 上传一个扩展名为 `.heic` 但 MIME 为 `image/heic` 的文件 |
| **预期结果** | 文件被接受并正常显示文件名；或按图片类型处理 |

### 4.6 大写扩展名文件处理

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证扩展名为大写的文件是否能正常识别 |
| **执行步骤** | 1. 访问首页 2. 上传文件名为 `REQUIREMENT.TXT` 的文件 |
| **预期结果** | 文件被正常接受（扩展名比较应忽略大小写）；显示文件名 |

### 4.7 混合大小写扩展名处理

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证扩展名大小写混合的文件是否能正常识别 |
| **执行步骤** | 1. 访问首页 2. 上传 `document.DocX` 文件 |
| **预期结果** | 文件被正常接受；扩展名比较大小写不敏感 |

### 4.8 文件名含多个点号的扩展名提取

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证文件名包含多个点号时，正确提取最后一个扩展名 |
| **执行步骤** | 1. 访问首页 2. 上传 `v1.2.3.requirement.txt` 文件 |
| **预期结果** | 正确提取扩展名 `.txt`；文件被正常接受 |

### 4.9 文件名不含点号的处理

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证文件名完全不含点号时的处理 |
| **执行步骤** | 1. 访问首页 2. 上传一个名为 `Makefile` 的文件（非图片 MIME） |
| **预期结果** | 显示"不支持的文件格式"错误；不会触发分析 |

### 4.10 空文件名处理

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证文件名为空或仅含扩展名时的健壮性 |
| **执行步骤** | 1. 访问首页 2. 尝试上传文件名为 `.txt`（只有扩展名无主体名）的文件 |
| **预期结果** | 程序不崩溃；根据扩展名命中 txt 解析器或给出合理提示 |

---

## 5. 分析流程（8 条）

### 5.1 上传文件后自动触发分析

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证选择有效文件后，自动触发 AI 分析，无需手动点击按钮 |
| **执行步骤** | 1. 访问首页 2. 上传 test_sample.txt 3. 观察页面变化 |
| **预期结果** | 文件选中后立即进入 loading 状态（不等待用户额外操作） |

### 5.2 分析中显示 loading 动画和文案

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证分析进行中显示加载动画和提示文案 |
| **执行步骤** | 1. 访问首页 2. 上传 test_sample.txt 3. 立即观察页面 |
| **预期结果** | 显示 loading spinner（旋转动画元素）；显示文案"正在解析文件，AI 分析需求内容..." |

### 5.3 分析中隐藏旧结果区域

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证 loading 期间不显示任何结果区域（需求摘要/流程图/表格/下载栏） |
| **执行步骤** | 1. 先完成一次分析（已有结果展示） 2. 清除文件后再次上传新文件 3. 在 loading 期间检查页面 |
| **预期结果** | 上传新文件后旧结果立即消失；loading 期间不显示任何 h2 标题；loading 期间不显示表格 |

### 5.4 分析完成后展示全部四个结果模块

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证分析完成后，四个结果模块（需求摘要、核心流程图、测试用例表格、下载操作栏）全部展示 |
| **执行步骤** | 1. 上传 test_sample.txt 2. 等待 loading 消失 3. 检查页面结果区域 |
| **预期结果** | 依次显示：需求摘要（含要点数量徽章）；核心流程图（含 SVG 节点和连线）；测试用例表格（含数据行）；下载操作栏（含"下载 Excel"和"复制表格"按钮） |

### 5.5 后端正常时返回完整数据结构

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证后端正常运行时，返回的数据包含所有必需字段 |
| **执行步骤** | 1. 确认后端服务 `localhost:8000` 正常运行 2. 上传 test_sample.txt 3. 等待分析完成 4. 检查结果数据结构 |
| **预期结果** | 返回数据包含 `summary`（数组，长度 > 0）；包含 `flowSteps`（数组，长度 > 0）；包含 `flowEdges`（数组，长度 > 0）；包含 `testCases`（数组，长度 > 0，每条含 id/category/title/preconditions/steps/expected） |

### 5.6 分析完成后 loading 状态清除

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证分析完成后，loading 动画和文案消失 |
| **执行步骤** | 1. 上传文件触发分析 2. 等待分析完成 3. 检查页面 |
| **预期结果** | loading spinner 消失；"正在解析文件，AI 分析需求内容..."文案消失 |

### 5.7 大文件分析不超时

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证上传较大文件（~1MB）后，分析在规定时间内完成 |
| **执行步骤** | 1. 生成一个约 1MB 的 .txt 文件 2. 上传该文件 3. 等待分析完成 |
| **预期结果** | 在 60 秒内完成分析（默认超时）；结果正常展示 |

### 5.8 仅空格/空白字符的文件分析

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证上传内容全是空白字符（空格/换行/制表符）的文件时，分析流程不崩溃 |
| **执行步骤** | 1. 上传一个内容为 `   \n  \t  \n   ` 的 .txt 文件 2. 等待分析完成 |
| **预期结果** | 分析流程正常完成（不报错、不崩溃）；返回测试用例表格（可能内容较少或为空） |

---

## 6. 需求摘要展示（6 条）

### 6.1 需求摘要标题与计数正确

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证需求摘要区域标题为"需求摘要"，并显示要点数量徽章 |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 查看需求摘要区域 |
| **预期结果** | 标题显示"需求摘要"；旁边徽章显示"X 条要点"（X = summary 数组长度）；图标为灯泡（Lightbulb） |

### 6.2 每条摘要前有对勾图标

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证每条需求摘要前有 CheckCircle2 对勾图标 |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 查看需求摘要列表 |
| **预期结果** | 每条摘要前有一个绿色/主题色对勾图标；图标与文字对齐 |

### 6.3 摘要内容为可读文本

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证需求摘要内容是完整、可读的中文描述 |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 逐条阅读需求摘要 |
| **预期结果** | 每条摘要为完整的中文句子；内容与上传的需求文件相关；无乱码/截断 |

### 6.4 空摘要时显示占位提示

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证后端返回空 summary 数组时的处理 |
| **执行步骤** | 1. 模拟后端返回 `summary: []` 2. 查看需求摘要区域 |
| **预期结果** | 显示"暂无需求摘要"占位文案；不显示空列表 |

### 6.5 摘要数量为 0 时不显示徽章

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证 summary 为空数组时不显示"0 条要点"徽章 |
| **执行步骤** | 1. 模拟后端返回空 summary | 2. 检查需求摘要区域 |
| **预期结果** | 不显示徽章或显示"0 条要点"；不崩溃 |

### 6.6 超长摘要文本不溢出

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证超长摘要文本（>200字符）不破坏页面布局 |
| **执行步骤** | 1. 模拟后端返回一条 300 字符的摘要 2. 查看页面布局 |
| **预期结果** | 文本自动换行；不超出卡片边界；不出现水平滚动条 |

---

## 7. 核心流程图（9 条）

### 7.1 流程图标题正确

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证流程图区域标题为"核心流程" |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 查看流程图区域 |
| **预期结果** | 显示 h2 标题"核心流程"；前方有 GitBranch 图标 |

### 7.2 流程节点数量大于 0

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证流程图包含至少 1 个流程节点 |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 统计流程节点数量 |
| **预期结果** | 节点数量 ≥ 1；每个节点显示其 label 文本 |

### 7.3 流程节点文字可读

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证每个流程节点上的文字清晰可读 |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 逐个检查节点文字 |
| **预期结果** | 所有节点文字无重叠；文字在节点边界内完整显示；无截断 |

### 7.4 SVG 连线（path）存在

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证流程图节点间有 SVG 连线（贝塞尔曲线） |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 检查 SVG 中的 path 元素数量 |
| **预期结果** | SVG path 数量 ≥ 1（至少 1 条连线）；连线颜色为淡紫色 `#b39ddb` |

### 7.5 箭头标记（marker）定义存在

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证 SVG 中定义了箭头标记（marker）用于连线末端 |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 检查 SVG 中的 marker 和 polygon 元素 |
| **预期结果** | SVG defs 中包含 `<marker id="arrowhead">`；marker 中包含三角形 polygon；箭头颜色为 `#b39ddb` |

### 7.6 无流程数据时显示占位

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证后端返回空 flowSteps 时的处理 |
| **执行步骤** | 1. 模拟后端返回 `flowSteps: []` 2. 查看流程图区域 |
| **预期结果** | 显示"暂无流程数据"占位文案；不渲染空 SVG |

### 7.7 空 flowEdges 时节点仍展示

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证 edge 为空但 steps 有数据时，节点正常显示 |
| **执行步骤** | 1. 模拟后端返回 `flowSteps: [{id:'1',label:'步骤1'}, {id:'2',label:'步骤2'}]` 但 `flowEdges: []` 2. 查看流程图 |
| **预期结果** | 两个节点正常渲染；无连线；页面不崩溃 |

### 7.8 节点 label 含换行符时正确换行

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证节点 label 包含 `\n` 时，在节点内正确换行显示 |
| **执行步骤** | 1. 模拟后端返回含换行符的 label（如 `"登录成功\n跳转首页"`） 2. 查看节点显示 |
| **预期结果** | 节点内文字正确换行为两行；不溢出节点边界 |

### 7.9 多分支结构渲染正确

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证有分支的流程图（一个节点指向多个子节点）正确渲染 |
| **执行步骤** | 1. 使用包含分支的测试数据（1→2, 1→3, 2→4, 3→4） 2. 查看流程图 |
| **预期结果** | 所有分支节点正确排列（BFS 布局）；分支节点可能偏移到右侧列；所有连线正确绘制 |

---

## 8. 测试用例表格（12 条）

### 8.1 表格标题与用例计数正确

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证表格标题为"测试用例"，并显示用例总数徽章 |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 查看表格标题区域 |
| **预期结果** | 标题显示"测试用例"（h2）；旁边徽章显示"X 条"（X = 实际用例总数） |

### 8.2 表格包含 6 列表头

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证表格完整包含 6 列：序号、分类、测试标题、前置条件、测试步骤、预期结果 |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 检查表头 th 元素 |
| **预期结果** | 表头依次为：#、分类、测试标题、前置条件、测试步骤、预期结果；共 6 列 |

### 8.3 每行数据包含 6 个单元格

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证每一行测试用例都包含完整的 6 列数据 |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 逐个检查 tbody 中每行 tr 的 td 数量 |
| **预期结果** | 每行的 td 数量为 6；不出现空行或列数异常 |

### 8.4 表格包含四种分类的用例

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证测试用例覆盖四种分类：核心流程、边界值、安全性、稳定性 |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 检查分类列的值分布 |
| **预期结果** | 至少存在 1 条"核心流程"分类用例；至少存在 1 条"边界值"分类用例；至少存在 1 条"安全性"分类用例；至少存在 1 条"稳定性"分类用例 |

### 8.5 分类徽章颜色正确

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证四种分类徽章使用正确的颜色 |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 检查各分类徽章的背景色 |
| **预期结果** | 核心流程：绿色（`#66bb6a`）；边界值：橙色（`#ffa726`）；安全性：红色（`#ef5350`）；稳定性：蓝色（`#42a5f5`） |

### 8.6 序号列连续递增

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证测试用例序号从 1 开始连续递增 |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 读取第一列所有序号 |
| **预期结果** | 序号从 1 开始；无跳号；无重复号 |

### 8.7 表格 hover 行高亮

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证鼠标悬停在表格行上时出现高亮效果 |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 鼠标悬停在某一行上 3. 观察背景色变化 |
| **预期结果** | 悬停行的背景色改变（通常变浅或出现阴影）；移开后恢复 |

### 8.8 表格横向滚动（窄屏幕）

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证在较窄的视口下，表格支持横向滚动 |
| **执行步骤** | 1. 将浏览器窗口宽度缩小到 800px 2. 上传文件并等待分析完成 3. 查看表格区域 |
| **预期结果** | 表格外层容器出现横向滚动条；所有列仍可完整查看；表头固定可见 |

### 8.9 空数据时显示占位

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证 testCases 为 null 时的处理 |
| **执行步骤** | 1. 模拟后端返回 `testCases: null` 2. 查看表格区域 |
| **预期结果** | 显示"暂无测试用例数据"占位文案；不渲染空表格 |

### 8.10 空数组时显示占位

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证 testCases 为空数组 `[]` 时的处理 |
| **执行步骤** | 1. 模拟后端返回 `testCases: []` 2. 查看表格区域 |
| **预期结果** | 显示"暂无测试用例数据"占位文案 |

### 8.11 前置条件为空时显示占位符

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证某条用例的 preconditions 为空时，表格中显示"—"占位符 |
| **执行步骤** | 1. 上传文件并等待分析完成 2. 找到 preconditions 为空的用例行 3. 查看该单元格 |
| **预期结果** | 显示"—"（破折号）占位符；不显示 undefined/null/空白 |

### 8.12 表格数据不出现 undefined/null 原始值

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证表格中所有可选字段（preconditions/steps/expected）为空时正确降级显示 |
| **执行步骤** | 1. 模拟后端返回缺少 steps 字段的用例 2. 查看该单元格 |
| **预期结果** | 显示"—"或空字符串；不显示 "undefined" 或 "null" 字符串 |

---

## 9. 分类筛选（10 条）

### 9.1 5 个分类 Tab 全部存在且含计数

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证表格上方显示 5 个分类筛选 Tab，各有数字徽章 |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 查看分类筛选 Tab 区域 |
| **预期结果** | 共 5 个 Tab；名称和计数依次为：全部 [N]、核心流程 [n1]、边界值 [n2]、安全性 [n3]、稳定性 [n4]；每个 Tab 包含 Filter 图标 |

### 9.2 默认选中"全部"Tab

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证页面初始加载后，默认选中"全部"分类 Tab |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 检查 Tab 选中状态 |
| **预期结果** | "全部"Tab 处于选中/高亮状态（active 样式）；表格显示所有分类的用例 |

### 9.3 点击"核心流程"筛选仅显示该类用例

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证点击"核心流程"Tab 后，表格仅显示核心流程分类的用例 |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 记录全部用例数 N 3. 点击"核心流程"Tab 4. 检查筛选后结果 |
| **预期结果** | 表格行数 ≤ 全部用例数 N；所有可见行的分类列均为"核心流程" |

### 9.4 点击"边界值"筛选仅显示该类用例

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证点击"边界值"Tab 后，表格仅显示边界值分类的用例 |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 点击"边界值"Tab 3. 检查筛选后结果 |
| **预期结果** | 所有可见行的分类列均为"边界值"；筛选后的行数 > 0 |

### 9.5 点击"安全性"筛选仅显示该类用例

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证点击"安全性"Tab 后，表格仅显示安全性分类的用例 |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 点击"安全性"Tab 3. 检查筛选后结果 |
| **预期结果** | 所有可见行的分类列均为"安全性" |

### 9.6 点击"稳定性"筛选仅显示该类用例

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证点击"稳定性"Tab 后，表格仅显示稳定性分类的用例 |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 点击"稳定性"Tab 3. 检查筛选后结果 |
| **预期结果** | 所有可见行的分类列均为"稳定性" |

### 9.7 筛选后切回"全部"恢复完整列表

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证从某个分类筛选切回"全部"Tab 后，表格恢复显示所有用例 |
| **执行步骤** | 1. 记录全部用例数 N 2. 点击"核心流程"Tab 3. 再点击"全部"Tab 4. 检查表格行数 |
| **预期结果** | 表格行数恢复为 N；与初始加载时一致 |

### 9.8 筛选后无匹配数据时显示占位

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证选中某个分类但无匹配数据时，表格区域显示提示 |
| **执行步骤** | 1. 模拟后端只返回"核心流程"分类的用例 2. 点击"安全性"Tab 3. 查看表格区域 |
| **预期结果** | 显示"该分类下暂无测试用例"提示；表格 tbody 无数据行 |

### 9.9 Tab 计数字与筛选结果一致

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证 Tab 上显示的数字徽章与实际该类用例数一致 |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 记录每个 Tab 上的数字 3. 逐一筛选并统计实际行数 4. 对比两组数据 |
| **预期结果** | 各 Tab 上的数字 = 筛选后实际显示的行数（全部 Tab 的数字 = 表格总行数） |

### 9.10 快速连续切换 Tab 无异常

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证快速连续点击不同 Tab 时，页面不崩溃且最终状态正确 |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 快速连续点击：核心流程 → 边界值 → 安全性 → 稳定性 → 全部 3. 检查最终状态 |
| **预期结果** | 页面无卡死/崩溃；无控制台报错；最终切换到的 Tab 对应筛选结果正确 |

---

## 10. Excel 下载（8 条）

### 10.1 点击"下载 Excel"触发文件下载

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证点击"下载 Excel"按钮后，浏览器触发 .xlsx 文件下载 |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 点击"下载 Excel"按钮 |
| **预期结果** | 浏览器下载一个文件；文件名为 `测试用例_YYYY-MM-DD.xlsx` 格式；文件大小 > 0 |

### 10.2 下载中按钮显示 loading 状态

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证下载进行中按钮文案变为"生成中..."并显示旋转图标 |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 点击"下载 Excel" 3. 立即观察按钮状态 |
| **预期结果** | 按钮文字变为"生成中..."；图标变为 Loader2（旋转动画）；按钮处于 disabled 状态 |

### 10.3 下载完成后按钮恢复正常

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证下载完成后，按钮恢复为"下载 Excel"可点击状态 |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 点击"下载 Excel" 3. 等待下载完成 4. 观察按钮 |
| **预期结果** | 按钮文字恢复为"下载 Excel"；图标恢复为 Download；按钮可再次点击 |

### 10.4 下载的 Excel 包含正确表头

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证下载的 .xlsx 文件第一行为 6 列表头 |
| **执行步骤** | 1. 完成分析并下载 Excel 2. 用 Excel/WPS 打开文件 3. 查看 Sheet1 第一行 |
| **预期结果** | 第一行表头为：序号 | 分类 | 测试标题 | 前置条件 | 测试步骤 | 预期结果 |

### 10.5 下载的 Excel 数据与页面表格一致

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证下载的 Excel 中数据与页面显示的测试用例完全一致 |
| **执行步骤** | 1. 记录页面表格显示的用例数量和内容 2. 下载 Excel 3. 打开对比 |
| **预期结果** | Excel 行数 = 页面表格行数；每条数据内容一一对应 |

### 10.6 下载的 Excel Sheet 名为"测试用例"

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证 Excel 文件的 Sheet 名称为"测试用例" |
| **执行步骤** | 1. 完成分析并下载 Excel 2. 打开文件查看 Sheet 标签 |
| **预期结果** | Sheet 标签显示为"测试用例" |

### 10.7 无测试用例时下载按钮不可见

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证 testCases 为空或 null 时，下载操作栏不显示 |
| **执行步骤** | 1. 访问首页（不传文件或清空结果） 2. 检查页面 |
| **预期结果** | 下载操作栏不渲染；不出现"下载 Excel"按钮 |

### 10.8 连续多次下载不报错

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证连续快速点击"下载 Excel"3次，不会出错或重复下载损坏文件 |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 连续点击"下载 Excel"3次 3. 检查下载的文件 |
| **预期结果** | 每次下载的文件均完整可用；无控制台报错；loading 状态正确切换 |

---

## 11. 复制表格（6 条）

### 11.1 点击"复制表格"将数据写入剪贴板

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证点击"复制表格"按钮后，测试用例数据以 TSV 格式写入系统剪贴板 |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 点击"复制表格" 3. 在任意文本编辑器中粘贴（Ctrl+V） |
| **预期结果** | 粘贴内容为 TSV 格式；第一行为表头（序号\t分类\t测试标题\t前置条件\t测试步骤\t预期结果）；后续行为数据行 |

### 11.2 复制后按钮变为"已复制"

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证复制成功后按钮文案变为"已复制"，图标变为 Check |
| **执行步骤** | 1. 上传 test_sample.txt 并等待分析完成 2. 点击"复制表格" 3. 立即观察按钮 |
| **预期结果** | 按钮文字变为"已复制"；图标变为 Check（对勾）；按钮颜色可能有变化 |

### 11.3 "已复制"状态 2 秒后自动恢复

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证"已复制"状态持续约 2 秒后自动恢复为"复制表格" |
| **执行步骤** | 1. 点击"复制表格" 2. 等待 2.5 秒 3. 观察按钮 |
| **预期结果** | 按钮文字恢复为"复制表格"；图标恢复为 Copy |

### 11.4 剪贴板内容与表格数据一致

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证复制到剪贴板的数据与表格显示的数据完全对应 |
| **执行步骤** | 1. 记录页面表格的行数和内容 2. 复制表格 3. 粘贴到 Excel/文本编辑器 4. 逐行对比 |
| **预期结果** | 粘贴的行数 = 表格行数 + 1（含表头行）；每条数据与表格一致 |

### 11.5 不支持 Clipboard API 时降级方案生效

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证在不支持 navigator.clipboard 的环境（如旧浏览器/非 HTTPS）中，降级方案（textarea + execCommand）生效 |
| **执行步骤** | 1. 在禁用 clipboard API 的环境中访问页面 2. 上传文件并完成分析 3. 点击"复制表格" |
| **预期结果** | 复制成功，按钮变为"已复制"；2 秒后恢复 |

### 11.6 无测试用例时复制按钮不可见

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证 testCases 为空时，复制按钮不显示 |
| **执行步骤** | 1. 访问首页（无结果状态） 2. 检查页面 |
| **预期结果** | 不出现"复制表格"按钮 |

---

## 12. 错误处理（9 条）

### 12.1 后端不可达时显示错误提示

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证后端服务未启动时，上传文件后显示合理的错误信息 |
| **执行步骤** | 1. 停止后端服务（`localhost:8000`） 2. 确认前端可访问 3. 上传 test_sample.txt |
| **预期结果** | loading 结束后显示红色错误横幅；错误信息包含"连接失败"或"分析失败"等提示；页面不崩溃 |

### 12.2 后端返回 500 错误时的处理

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证后端返回 HTTP 500 错误时前端的错误展示 |
| **执行步骤** | 1. 模拟后端分析接口返回 500 状态码 2. 上传文件 |
| **预期结果** | 前端显示错误横幅；显示后端返回的 error detail 信息；结果区域不显示 |

### 12.3 后端返回非 JSON 响应时的处理

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证后端返回非 JSON 格式响应（如 HTML 502 页面）时前端不崩溃 |
| **执行步骤** | 1. 模拟后端返回纯文本/HTML 错误页面 2. 上传文件 |
| **预期结果** | 前端捕获错误并显示错误横幅；不抛出未处理的 JSON 解析异常 |

### 12.4 错误横幅出现后可清除文件重试

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证出现错误后，可以清除文件并重新上传来恢复 |
| **执行步骤** | 1. 触发一次错误（如上传不支持的格式） 2. 点击清除按钮 3. 上传有效文件 |
| **预期结果** | 错误横幅消失；新文件正常上传；分析流程正常启动并完成 |

### 12.5 网络中断时上传行为的处理

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证上传过程中网络断开时的处理 |
| **执行步骤** | 1. 上传文件 2. 在分析请求发出后，断开网络（Chrome DevTools → Network → Offline） |
| **预期结果** | 最终显示错误提示；loading 状态清除；不无限等待 |

### 12.6 后端响应超长的处理

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证后端分析耗时过长（超时）时的处理 |
| **执行步骤** | 1. 模拟后端 sleep 120 秒 2. 上传文件 3. 观察 |
| **预期结果** | 前端请求在合理时间内超时（通常 30-60s）；显示超时错误提示；loading 状态清除 |

### 12.7 后端返回 code !== 0 的错误响应

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证后端返回成功 HTTP 状态但业务 code !== 0 时的错误处理 |
| **执行步骤** | 1. 模拟后端返回 `{"code": -1, "message": "业务错误"}` 2. 上传文件 |
| **预期结果** | 前端显示"业务错误"错误横幅；不显示结果区域 |

### 12.8 response.json() 解析失败的兜底

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证后端返回非标准 JSON 错误响应时，使用 statusText 作为错误信息 |
| **执行步骤** | 1. 模拟后端返回 HTTP 400，body 为非 JSON 2. 上传文件 |
| **预期结果** | 错误横幅显示 `请求失败 (400)` 格式的错误信息；前端不抛出 JSON 解析异常 |

### 12.9 后端返回空文件体的处理

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证后端返回空 body 的 200 响应时的处理 |
| **执行步骤** | 1. 模拟后端返回 `200 OK` 但 body 为空 2. 上传文件 |
| **预期结果** | 前端捕获 JSON 解析异常；显示合理错误提示；不崩溃 |

---

## 13. 状态管理与交互（8 条）

### 13.1 清除按钮功能正常

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证点击清除（X）按钮后，恢复到初始状态 |
| **执行步骤** | 1. 上传文件并等待分析完成（有完整结果） 2. 点击文件旁的 X 清除按钮 |
| **预期结果** | 上传区域恢复默认引导文案；结果区域全部消失；空状态提示重新显示；无 loading 状态残留 |

### 13.2 上传新文件替换旧结果

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证有分析结果时上传新文件，旧结果被清除并展示新分析结果 |
| **执行步骤** | 1. 上传文件 A 并等待分析完成 2. 清除后上传文件 B 3. 等待分析完成 |
| **预期结果** | 旧结果 A 在上传 B 后立即清除；新结果 B 正确展示；标题/内容与文件 B 对应 |

### 13.3 上传同类型不同内容文件

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证先后上传两个 .txt 文件但内容不同时，结果分别正确 |
| **执行步骤** | 1. 上传 test_sample.txt 并记录结果 2. 清除后上传另一个内容不同的 .txt 文件 3. 对比两次结果 |
| **预期结果** | 两次分析结果不同（或分别对应各自文件内容）；无数据串扰 |

### 13.4 上传不同类型文件均正常

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证先后上传 .txt → .md → .docx → .pdf → .xmind → 图片，每种类型均能正常分析 |
| **执行步骤** | 1. 依次上传不同格式的有效文件 2. 每次等待分析完成后清除再传下一个 |
| **预期结果** | 每种格式均正常触发分析流程；结果正确展示；无格式串扰 |

### 13.5 loading 状态与 disabled 联动

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证 loading 期间上传区域的 disabled 属性生效，阻止用户操作 |
| **执行步骤** | 1. 上传文件触发 loading 2. 尝试点击上传区域 3. 尝试拖拽文件到上传区域 |
| **预期结果** | 点击上传区域无效（不弹文件选择框）；拖拽不触发高亮；拖拽释放不触发上传 |

### 13.6 页面无 JS 异常（含操作后）

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证完整操作流程（上传→等待→筛选→下载→复制→清除）后，控制台无 JS 异常 |
| **执行步骤** | 1. 打开控制台 2. 执行完整操作流程 3. 检查控制台输出 |
| **预期结果** | Console 面板无红色错误（error 级别）；无未捕获的 Promise rejection |

### 13.7 组件卸载无内存泄漏

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证分析结果出现后，清除文件再重新上传，无内存泄漏 Warning |
| **执行步骤** | 1. 打开控制台 2. 上传 → 清除 → 上传 → 清除 → 上传（重复 5 次） 3. 检查控制台 |
| **预期结果** | 无 "Can't perform a React state update on an unmounted component" 警告；无内存泄漏提示 |

### 13.8 浏览器前进/后退不破坏状态

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证浏览器前进/后退按钮操作后页面正常 |
| **执行步骤** | 1. 访问首页 2. 上传文件完成分析 3. 刷新页面 4. 点击浏览器后退 |
| **预期结果** | 页面不崩溃；状态重置为初始状态（因为是 SPA 无路由变化） |

---

## 14. 后端 API（10 条）

### 14.1 健康检查接口正常

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证 GET /api/health 返回正常状态 |
| **执行步骤** | 1. 启动后端服务 2. 使用 curl/浏览器访问 `http://localhost:8000/api/health` |
| **预期结果** | 返回 HTTP 200；响应 JSON 包含 `{"status": "ok"}` 或类似健康状态字段 |

### 14.2 分析接口接受 .txt 文件

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证 POST /api/analyze 接受 .txt 文件并返回完整分析结果 |
| **执行步骤** | 1. 使用 curl 发送 POST 请求：`curl -F "file=@test_sample.txt" http://localhost:8000/api/analyze` |
| **预期结果** | HTTP 200；响应 JSON 中 `code: 0`；`data.summary` 数组非空；`data.testCases` 数组非空；`data.flowSteps` 数组非空 |

### 14.3 分析接口接受 .md 文件

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证后端能正确解析 .md 文件并返回分析结果 |
| **执行步骤** | 1. 准备一个 .md 需求文件 2. 通过 API 上传分析 |
| **预期结果** | HTTP 200；code 为 0；返回完整分析结果 |

### 14.4 分析接口接受 .docx 文件

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证后端能正确解析 .docx 文件（使用 python-docx/mammoth）并返回分析结果 |
| **执行步骤** | 1. 准备一个 .docx 需求文档 2. 通过 API 上传分析 |
| **预期结果** | HTTP 200；code 为 0；返回完整分析结果 |

### 14.5 分析接口接受 .pdf 文件

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证后端能正确解析 .pdf 文件并提取文本分析 |
| **执行步骤** | 1. 准备一个文本型 .pdf 需求文档 2. 通过 API 上传分析 |
| **预期结果** | HTTP 200；code 为 0；返回完整分析结果 |

### 14.6 分析接口接受 .xmind 文件

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证后端能正确解析 .xmind 文件（解压 content.json 提取 topic 树）并返回分析结果 |
| **执行步骤** | 1. 准备一个 .xmind 思维导图文件 2. 通过 API 上传分析 |
| **预期结果** | HTTP 200；code 为 0；返回完整分析结果 |

### 14.7 上传不支持格式返回 400

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证后端收到不支持格式的文件时返回 HTTP 400 错误 |
| **执行步骤** | 1. 使用 curl 发送一个 .exe 文件 |
| **预期结果** | HTTP 400；响应 JSON 包含 `detail` 字段说明错误原因 |

### 14.8 上传空文件返回 400

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证后端收到空文件（0 字节）时返回 HTTP 400 错误 |
| **执行步骤** | 1. 创建 0 字节的空文件 2. 通过 API 上传 |
| **预期结果** | HTTP 400；响应 JSON 包含 `"上传的文件为空"` 或类似错误描述 |

### 14.9 CORS 头正确设置

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证后端响应头包含正确的 CORS 设置，允许前端跨域请求 |
| **执行步骤** | 1. 从 `http://localhost:5173` 发起跨域 POST 请求 2. 检查响应头 |
| **预期结果** | `Access-Control-Allow-Origin` 包含 `http://localhost:5173`；`Access-Control-Allow-Methods` 包含 POST/GET/OPTIONS；预检请求 OPTIONS 正常响应 |

### 14.10 请求体不含文件时返回 422

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证 POST /api/analyze 未附带文件时返回 HTTP 422 验证错误 |
| **执行步骤** | 1. 发送不带 file 参数的 POST 请求 |
| **预期结果** | HTTP 422；FastAPI 自动返回请求体验证错误描述 |

---

## 15. 兼容性与性能（7 条）

### 15.1 Chrome 最新版兼容

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证在 Chrome 最新版浏览器中页面功能完整可用 |
| **执行步骤** | 1. 使用 Chrome 最新版打开页面 2. 执行完整操作流程 3. 检查所有功能 |
| **预期结果** | 全部功能正常：上传、分析、表格、筛选、下载、复制均可用；UI 无错位 |

### 15.2 Edge 最新版兼容

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证在 Edge 最新版浏览器中页面功能完整可用 |
| **执行步骤** | 1. 使用 Edge 最新版打开页面 2. 执行完整操作流程 |
| **预期结果** | 全部功能正常 |

### 15.3 Firefox 最新版兼容

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证在 Firefox 最新版浏览器中页面功能完整可用 |
| **执行步骤** | 1. 使用 Firefox 最新版打开页面 2. 执行完整操作流程 |
| **预期结果** | 全部功能正常（注意：Firefox 的 DragEvent 与 Chrome 略有差异，拖拽功能需额外验证） |

### 15.4 页面首次加载时间

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证页面首次加载（含 JS Bundle）在合理时间内完成 |
| **执行步骤** | 1. 打开 Chrome DevTools → Network 面板 2. 勾选 "Disable cache" 3. 访问首页 4. 记录 DOMContentLoaded 和 Load 时间 |
| **预期结果** | 首次 Load 时间 < 3 秒（本地开发环境）；核心 JS bundle < 500KB（gzip 后） |

### 15.5 分析响应时间

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证从文件上传到分析结果展示的端到端耗时在可接受范围内 |
| **执行步骤** | 1. 上传 test_sample.txt 2. 记录从点击上传到结果展示完成的时间 |
| **预期结果** | 使用规则引擎回退时 < 5 秒；使用 AI API 时 < 30 秒 |

### 15.6 页面内存占用

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证长时间使用（多次上传分析）后页面内存占用不持续增长 |
| **执行步骤** | 1. 打开 Chrome Task Manager（Shift+Esc） 2. 重复"上传→分析→清除"操作 10 次 3. 监控内存变化 |
| **预期结果** | 内存占用稳定在一定范围内；无持续增长趋势（无内存泄漏） |

### 15.7 并发请求处理

| 项目 | 内容 |
|------|------|
| **用例标题** | 验证连续快速上传文件时，仅处理最后一次请求的结果 |
| **执行步骤** | 1. 快速连续上传 3 个不同文件（间隔 < 500ms） 2. 等待分析完成 3. 检查展示结果 |
| **预期结果** | 最终展示的是最后一个文件的分析结果；不出现多个结果叠加；无竞态条件导致的 UI 异常 |

---

## 附录

### A. 测试环境要求

| 项目 | 要求 |
|------|------|
| 前端地址 | `http://localhost:5173` |
| 后端地址 | `http://localhost:8000` |
| 浏览器 | Chrome / Edge / Firefox 最新版 |
| 测试数据目录 | `requirement-analyzer/tests/test-data/` |
| Node.js | ≥ 18.x |
| Python | ≥ 3.9 |

### B. 测试数据文件清单

| 文件 | 用途 |
|------|------|
| `test_sample.txt` | 标准测试文件（密码重置需求） |
| `test_sample.md` | Markdown 格式测试文件 |
| `test_sample.docx` | Word 文档测试文件 |
| `test_sample.pdf` | PDF 测试文件 |
| `test_sample.xmind` | XMind 思维导图测试文件 |
| `test_sample.png` | 图片测试文件 |
| `empty.txt` | 空内容文件（边界测试） |
| `large.txt` | 大文件（~1MB，性能测试） |
| `invalid.exe` | 不支持格式文件（错误处理测试） |
| `only-spaces.txt` | 仅含空白字符文件（边界测试） |

### C. 测试用例分类说明

| 分类 | 含义 | 颜色 |
|------|------|------|
| 核心流程 | Happy Path 主流程验证 | 绿色 `#66bb6a` |
| 边界值 | 输入边界、极限值、空值测试 | 橙色 `#ffa726` |
| 安全性 | 注入、权限、数据校验 | 红色 `#ef5350` |
| 稳定性 | 并发、超时、异常恢复、资源管理 | 蓝色 `#42a5f5` |
